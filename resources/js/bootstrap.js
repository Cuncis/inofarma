/**
 * Application bootstrap.
 *
 * Kept dependency-free on purpose: the UI screens are presentational and do not
 * issue XHR requests yet. Wire up an HTTP client here when the screens are
 * connected to the backend.
 */
